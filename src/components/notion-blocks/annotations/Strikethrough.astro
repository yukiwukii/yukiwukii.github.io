---
import type { RichText } from "@/lib/interfaces.ts";

export interface Props {
	richText: RichText;
}

const { richText } = Astro.props;
---

{
	richText.Annotation.Strikethrough ? (
		<span class="annotation-strikethrough">
			<slot />
		</span>
	) : (
		<slot />
	)
}
