---
import type { MarkdownHeading } from "astro";

import { generateToc } from "src/utils/generateToc";

interface Props {
	headings: Array<MarkdownHeading>;
}

const { headings } = Astro.props;

const toc = generateToc(headings);
---

<aside class="fixed bottom-0 left-0 mx-2 my-0.5 z-30 w-[96%] max-w-[96%] whitespace-normal overflow-hidden sm:hidden bg-bgColor border border-accent-2/40 p-2 vopacity-0 rounded-lg visible-transition shadow-lg shadow-accent-2/40" id="bottom-toc-nav">
  {
      !!toc.length && (
          <>
              <p class="text-xs inline">
                  {toc.map((heading, index) => (
                      heading.depth == 1 && (
                          <a
                              class={`no-underline hover:text-accent ${heading.slug === "autogenerated-interlinked-content" ? "hidden" : ""}`}
                              href={`#${heading.slug}`}
                              id={`-bottomtocid--${heading.slug}`}
                              aria-label={`Scroll to section: ${heading.text}`}
                          >
                              {heading.text}{index < toc.length - 1 ? " • " : ""}
                          </a>
                      )
                  ))}
              </p>
          </>
      )
  }
</aside>
{/*uses style because apparently visibility+opacity cannot be transitioned in tailwind? */}
<style>
.visible-transition {
    transition: opacity 150ms linear, visibility 0ms linear 150ms;
}

.vopacity-0 {
    opacity: 0;
    visibility: hidden;
}

.vopacity-100 {
    opacity: 1;
    visibility: visible;
    transition-delay: 0s; /* No delay when showing */
}
</style>
